server {
    proxy on;
    listen 25;
    protocol smtp;
    starttls only;
    proxy_smtp_auth on;

    xclient on;
    server_name smtp.nginx;
}

server {
    proxy on;
    listen 465 ssl;
    protocol smtp;
    proxy_smtp_auth on;

    xclient on;
    server_name smtps.nginx;
}
